import React, { FC } from "react";
import { Text, View } from "react-native";

const TrendingCard: FC<TrendingCardProps> = ({ movie, index }) => {
	return (
		<View>
			<Text>TrendingCard</Text>
		</View>
	);
};

export default TrendingCard;
